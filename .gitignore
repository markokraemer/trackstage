node_modules
.DS_Store
dist
dist-ssr
*.local
.env*
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# typescript
*.tsbuildinfo

# convex
!.env.example
# Public build-time config for production deploys (no secrets) — see the file header.
!.env.production
tests/e2e/.auth/
tests/e2e/.results/
