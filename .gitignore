node_modules
.DS_Store
dist
dist-ssr
*.local
.env*
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Capture scratch — stills + intermediate video for the screenshot/GIF scripts.
# Kept on disk between runs so `capture-tour-gif.mjs --gif` can re-assemble
# without re-driving the browser; never committed (the GIF itself is).
.screenshot-frames/
.tour-frames/

# typescript
*.tsbuildinfo

# convex
!.env.example
# Public build-time config for production deploys (no secrets) — see the file header.
!.env.production
# Same, for the dev.trackstage.app staging Worker (deploy-dev.yml).
!.env.staging
tests/e2e/.auth/
tests/e2e/.results/
playwright-report/
