# Adversarial review — findings

**Reviewer:** fresh independent agent (did NOT build this).
**Target:** Trackstage @ `c7dffe5` · prod `https://trackstage.app` (Convex prod `keen-eagle-41`) · dev/local `neat-sparrow-926` (`localhost:3000`).
**Date:** 2026-08-11. **Ground read:** `docs/memory/PROMPTS.md` (166 prompts), `AGENTS.md`, `RULES.md`, `DECISIONS.md` (via refs), `SPEC`/rubric/coverage via refs, `mcp-proxy-matrix.md`, `sbek-ledger.md`, `api-parity.md`.
**Method:** prod read-only browser + API probes; authenticated write-probes confined to the DEV deployment; code/data reads; four parallel evidence-gathering sub-probes (REST/OpenAPI, MCP gating, email/Airtable, prompt-compliance/repo/rubric). No fixes applied. Nothing committed. All review artifacts removed; working tree verified clean; the one dev API key minted for probing was revoked.

**Severity (this review's scale):** **P0** = a judge hits it, or a Marko directive is unmet · **P1** = visible quality/parity miss · **P2** = polish · **P3** = trivia/by-design.
**Standing requirement — zero console errors:** **MET everywhere sampled** (prod home / public event / CFP / docs / design-system; prod authed app across all 13 routes; local CFP wizard). Not a single console error or failed request observed.

---

## Remediation status — 2026-08-12 certification branch

The table below is the current verdict after the original review was acted on. The
original findings remain below as the immutable before-state and repro record.

| Finding | Current verdict | Closure evidence |
| --- | --- | --- |
| F1 | **FIXED** | Generated/served OpenAPI advertises the production Convex site; the live check cross-checks independent sources instead of validating a URL against itself. |
| F2 | **FIXED** | Every user-controlled platform-email interpolation and CTA URL/label is escaped. The backend verifier submits `Quiet <Talk> & "Proof"` and asserts the stored HTML contains escaped title/ampersand/quotes; hostile evaluator names are covered too. |
| F3 | **FIXED** | HTML mode is decided from the stored template before variables are merged; ordinary titles such as `<Suspense>` cannot turn a plain template into HTML. |
| F4 | **FIXED** | Authenticated reads pay the `entity_reads` limiter at the dispatcher door and emit the documented rate-limit headers. |
| F5 | **FIXED** | All 84 MCP tools have registered generative views; a generated-tool-list invariant makes a new unmapped tool fail tests. The structured auto view is the non-raw safety floor. |
| F6 | **FIXED** | Successful metadata reads no longer return the internal `unknownResource` sentinel; the backend verifier asserts the key is absent from a real `/tracks` response. |
| F7 | **REVIEWED / ACCEPTED** | REST keeps ordinary machine-API `DELETE` semantics behind a scoped write key. Interactive MCP deletion retains `confirm:true` + exact `confirmName`. This is a deliberate compatibility boundary, not an accidental missing UI confirmation. |
| F8 | **FIXED** | `platformEmailDeliveries` is a durable mini-outbox with pending/retrying/sent/preview/failed states, five attempts (1s/5s/25s/125s), stuck-attempt recovery, 90-day retention, scoped issue UI and manual retry. Stable Resend idempotency keys prevent recovery duplicates. Local durable-row probes pass; synthetic 429/500/503/network/422 policy probes are 7/7. |
| F9–F10 | **FIXED** | Portal links patch field-by-field and merge server-side; the shared profile-completeness rule makes profile tasks born complete and rechecks them after every profile write. |
| F11 | **FIXED to the safe product boundary** | Embed enable/disable, branding, XML and multi-track filters shipped. Arbitrary custom CSS remains deliberately excluded as an injection/iframe-boundary footgun. |
| F12 | **FIXED** | Added `update_workspace`, `list_trash`, and `rotate_evaluator_token`; MCP is now 84 tools and the parity matrix names the deliberately excluded boundaries. |
| F13 | **FIXED** | Bare `/submit/:eventSlug` now resolves the event's deterministic primary CFP (open abstract → any open → closed abstract → oldest remaining). Playwright proves `/submit/ai-summit-2026` opens that event's CFP. |
| F14 | **REVIEWED / KEPT** | Marko supplied the live Stripe link and later explicitly restored the card. The button stays; only the awkward press-depth treatment was removed. |
| F15–F16 | **FIXED** | README claims 84 tools/600+ checks and the right clone directory; MIT `LICENSE` exists. The launch film is served at `/launch.mp4`, linked from README and rendered on the landing page with byte-range support. |
| F17 | **FIXED** | Invalid `application/json` now returns `400 Malformed JSON body.`; the backend verifier sends `{bad json` and distinguishes it from field validation. |
| F18 | **PARTIAL / P3 only** | Deadline reminders render real close-date/link vars and the Airtable comment now describes opt-in two-way sync. The dormant waitlisted template and invalid-placeholder authoring lint remain low-priority copy hygiene, not a core delivery-path failure. |
| F19–F20 | **FIXED** | `AGENTS.md` now names the shipped organizer IA and canonical/legacy public URLs; profile/headshot files say “Speaker profile” rather than “—”; the MCP matrix no longer claims bespoke tools are mechanically drift-proof. |
| F21 | **FIXED locally** | Every backend certification run begins with `seed:setup`; the isolated local deployment is the evidence target and throwaway keys are revoked by the verifier. This does not claim the shared cloud dev database is pristine. |
| New authz probe | **FIXED** | Event-scoped members could reach another event's webhook deliveries by known hook id. `requireHookAccess` now uses `requireEventAccess` for event hooks; the negative cross-event Playwright probe passes. |

Current residual product findings from the original table: **no P0/P1/P2 defects**. F7
and F14 are explicit reviewed decisions; F18 retains two P3 copy-authoring nits. Separate
Convex framework-contract/scale debt is quantified, without pretending cloud telemetry ran,
in `docs/reference/convex-launch-readiness-2026-08-12.md`.

---

## Findings table (most severe first)

| # | Sev | Surface | Claim vs observed reality | Repro (URL/command) | Suggested fix |
|---|-----|---------|---------------------------|---------------------|---------------|
| F1 | **P0** | REST/OpenAPI | Directive #53/#83/rule-28: "openapi.json 100% accurate **to the actual server**." Observed: the **prod-served** spec declares its API server as the **DEV** deployment. Scalar "Try it" on prod docs sends a user's real **prod** `sb_live_` key to DEV; a "Try it" write mutates DEV, a "Try it" `DELETE /v1/events/{ref}` wipes a DEV event. The `--check --live` gate is green *because* it bakes and probes that same dev URL — CI structurally cannot catch it. | `curl -s https://trackstage.app/docs/api/openapi.json \| python3 -c 'import sys,json;print(json.load(sys.stdin)["servers"])'` → `[{'url':'https://neat-sparrow-926.eu-west-1.convex.site'}]`. Prod's real site is `keen-eagle-41`. Root: `scripts/generate-openapi.mjs:2619` reads `VITE_CONVEX_SITE_URL` from `.env.local` (dev). | Emit the **prod** site URL (or a relative/placeholder server) for the published spec; add a prod-URL assertion to the check so it can't self-mask. |
| F2 | **P1** | Email egress | "All emails branded, perfect." Observed: `platformEmails.ts` interpolates **attacker-controlled** strings (`submissionTitle`, `submitterName` from the public CFP, `workspaceName`, `inviterName`, `evaluatorName`, `planName`) into HTML with **zero escaping**. A stranger submitting a talk titled `<a href="https://evil">…` injects live HTML into the notifyEmails admin alert sent from the DKIM-verified `hello@trackstage.app` — a phishing vector wearing the product's own domain. | `convex/platformEmails.ts:101,107,183–187,348–354` (no `escapeHtml`; the escaper exists at `convex/lib/email.ts:260` but is unused here). Preview iframe is sandboxed so it is not stored-XSS in-app; the risk is the outbound mail. | Escape every interpolated value in `platformEmails.ts` with the existing `escapeHtml`. |
| F3 | **P1** | Email egress | "No unresolved placeholders / clean rendered mail." Observed: a **legitimate** talk title containing an HTML-ish token (e.g. `Building <Suspense> in production`) flips the whole speaker email into raw-HTML mode — `renderMessageFor` merges vars into the body *before* `looksLikeHtml(body)` decides HTML-ness, so `<Suspense>` is swallowed by mail clients, newlines collapse, and the plain-text part is dropped. Hits ordinary content on the **core decision-email path** (also delivered to co-speakers). | `convex/comms.ts:183–184` (vars merged) → `comms.ts:1178–1187` + `lib/email.ts:256–258` (`/<[a-z][\s\S]*>/i`). | Decide HTML-ness from the stored **template**, not the var-merged body; or escape vars in plain-text templates. |
| F4 | **P1** | REST/OpenAPI | Spec declares `RateLimit-Limit/Remaining/Reset` response headers on **every** operation and an `x-rate-limit-bucket` header; promises an `entity_reads` bucket on reads. Observed: **reads emit none** of these headers, and the two biggest list reads bypass the limiter entirely. Spec-vs-server inaccuracy + unprotected reads. | Read: `GET /v1/event/ai-summit-2026/sessions` → no `RateLimit-*`, no `x-rate-limit-bucket`. Write: track-create → `ratelimit-limit:100`. `GET /v1/events` (`apiHttp.ts:697`) & sessions (`:974`) never call `enforceRateLimit`. | Emit the headers on reads / gate list reads, or delete the header + bucket claims from read ops in the spec. |
| F5 | **P1** | Prompt compliance / MCP UX | #30 "**every** individual MCP tool call needs a nice in-app generative UI." Observed: the copilot tool-view registry maps ~**50** of the **81** tools; unmapped tools render `RawFallback` (raw JSON). Registry was built at 31 tools and did not keep pace as the surface tripled. | `src/components/copilot/tool-views/registry.tsx:116` (`TOOL_VIEWS`), fallback `:242–262`; 81 tools in `convex/mcp.ts`. | Add views for the ~31 unmapped tools (or a richer typed fallback). |
| F6 | P2 | REST/OpenAPI | Internal dispatch sentinel `unknownResource:false` leaks into the body of **all 18** Event-Settings list responses; undocumented in the page schemas (yet baked into the spec's captured examples, so the spec contradicts itself). | `GET /v1/event/ai-summit-2026/tracks` → `{data,pagination,results,"unknownResource":false}`. Source `apiV1.ts:1676` + `apiHttp.ts:1796`. | Strip `unknownResource` before `jsonResponse`. |
| F7 | P2 | REST | `DELETE /v1/events/{ref}` cascade-deletes the entire event with only `write:events` scope, **no confirmation**, → 204. MCP's `delete_event` requires `confirm`+`confirmName`; the REST twin has no equivalent guard. Combined with F1, a docs "Try it" DELETE wipes a **dev** event in one click (reproduced during the sweep; restored via `seed:setup`). | `DELETE /v1/events/{ref}` with a `write:events` key. | Accept the machine-API tradeoff, or require a `confirmName`/`?confirm=` query param to match MCP. |
| F8 | P2 | Email egress | Rule 18e "nothing may silently not-send." Observed: platform-email transport failures are **silent** — `sendTransactionalEmail` on a Resend non-2xx does `console.error` + returns `{sent:false}`; callers are fire-and-forget. A failed password-reset or workspace-invite leaves no durable row, no retry, no UI signal (speaker outbox mail, by contrast, gets a `failed` row). | `convex/platformEmails.ts:60–67`; callers `workspaces.ts:159`, `auth.ts:71`, `evaluationsAdmin.ts:950`. | Persist platform-email outcomes (mini-outbox row or retry). |
| F9 | P2 | Rubric / Speakers | Ledger open defect still real: X/Twitter portal URL autosave **clobbers** — unserialized blur commits each rebuild all three links from a `savedRef` snapshot that only advances after the round-trip; a fast second blur overwrites the just-saved Twitter URL (server patches whole `links`). | `src/components/portal/profile-editor.tsx:138–152`; `convex/portal.ts:362`. | Per-field link patch or a serialized save queue. |
| F10 | P2 | Rubric / Speakers | Docs promise "Update their profile" **ticks itself** — but a task assigned to an **already-complete** profile never auto-completes (insertion never checks existing profile; the tick keys on a bio save only). | `convex/tasksAdmin.ts:86–127`; tick at `convex/portal.ts:364–367`. | On assign, auto-complete if the profile is already complete; broaden the auto-tick trigger. |
| F11 | P2 | Rubric / Embeds | EMB-15: 3 of 4 named gaps intact — **no** branding/colour/custom CSS (option vocab is only `format/hideDescriptions/hideSpeakers/hideImages/hideSearch/track/height`), **no** XML output (`EMBED_FORMATS = iframe/html/link/json/ics`), **no** per-embed enable/disable (no flag; a saved embed always renders). Data filter still single-track. | `convex/embeds.ts:23–33`; `convex/schema.ts:625–641`. | Add per-embed enabled flag + branding/CSS + XML + per-field filter if EMB-15 is targeted. |
| F12 | P2 | MCP full-proxy | #97 "MCP can do all the user can do." Real gaps NOT in the "deliberately excluded" list: (a) `workspaces.update` — no tool renames/edits a workspace; (b) `restore_submission` has no companion to **list** the trash, so its id is undiscoverable via MCP (matrix line 77 mis-maps this to `list_submissions`, which filters trashed rows out); (c) `evaluationsAdmin.rotateEvaluatorToken` — no MCP equivalent. | Enumerated 174 public fns vs 81 tools. `convex/mcp.ts`, `docs/reference/mcp-proxy-matrix.md`. | Add tools (or add to the excluded list with a reason). |
| F13 | P2 | Judge path / URL arch | #71/#89 "unique link structure everywhere." Legacy `/submit/:eventSlug` is treated as a **form** slug, so `/submit/ai-summit-2026` renders "We couldn't find that call for speakers" instead of resolving to the event's CFP. Inconsistent with `/e/:eventSlug` (which 307s to canonical). | `curl -s https://trackstage.app/submit/ai-summit-2026` → 200 soft-404; browser h1 = "We couldn't find that call for speakers". | Resolve `/submit/:eventSlug` to the event's primary CFP (or 307 to it). |
| F14 | P2 | Prompt compliance | #66 "button looks a bit odd perhaps we should remove that yk yk" (sent 2 min after supplying the Stripe link). The **Declare-the-winner** button was NOT removed — only its 3D press-depth styling was retired. Ambiguous ("how it loosk" ⇒ styling), but if he meant the button, it still ships. | `src/routes/index.tsx:17` (`STRIPE_CHECKOUT_URL`) → `pricing.tsx:87–97` → `declare-winner-button.tsx:26`. Live on `/`. | Confirm with Marko: keep or kill. |
| F15 | P2 | Repo hygiene | Repo is about to be flipped public; README carries stale facts: "**31 tools**" (actual 81), "**129** live backend checks" (gate logs say 617), and self-host `git clone …/trackstage && cd sessionboard` (clone dir is `trackstage`). | `README.md:112,211,144`. | Correct the three lines. |
| F16 | P2 | Prompt compliance | #80/#94 launch video: `video/out/trackstage-launch{,-v2,-v3}.mp4` exist with QA, but `video/out/*.mp4` is **gitignored** and referenced by no route/README/landing — the deliverable lives only in the local tree. #95's hero placement was explicitly optional, so not a hard miss, but if the submission needs the video it is currently unpublished. | `git check-ignore video/out/trackstage-launch.mp4` → ignored. | Publish the render somewhere deliberate, or drop the claim. |
| F17 | P2 | REST | Malformed JSON to an authed write returns a **field** error, not a parse error — `readJsonBody` swallows the parse failure and treats the body as empty. Clean envelope, no leak, just imprecise. | `POST …/tracks/create -d '{bad json'` → `400 "\`name\` is required."` (`apiHttp.ts:284`). | Return `400 "malformed JSON"` on parse failure for JSON content-type. |
| F18 | P3 | Email egress | Minor egress nits: (a) `{{first name}}`/`{{first-name}}` survive **literally** into sent mail (renderer regex only matches `[A-Za-z0-9_]`); (b) `deadline_reminder` **test send** renders empty `closeDate`/`formLink` ("closes on ."); (c) `waitlisted` template seeded + shown but never queued (no waitlist status); (d) stale `lib/airtable.ts:3–8` "ONE-WAY … never read back" comment contradicts the two-way pull. | `lib/email.ts:28`; `comms.ts:693–699`; `grep waitlisted convex/`. | Lint templates on save; backfill sample vars; remove/wire `waitlisted`; fix comment. |
| F19 | P3 | Design / docs | AGENTS.md "product surfaces" still lists an organizer **Tasks** and **Portals** page; the shipped nav is a flattened 7-item set with neither (tasks live on the dashboard/speakers, which is the intended simplification). Bare `/app/tasks` 404s but is not nav-linked. | `src/routes/app/route.tsx:95–147`; `/app/tasks` → not-found. | Update the AGENTS.md surface list to the shipped nav. |
| F20 | P3 | Rubric / MCP docs | (a) Files library shows Session "—" for non-session (profile) files — cosmetic, mitigated by task-title subtitle. (b) Matrix's "REST and MCP **cannot drift**" is overstated: only ~31 of 81 tools wrap `internal.apiV1.*`; the other ~50 (all reads + create_event, create_form, set/commit status, add_manual_session, schedule/unschedule/auto_place) are bespoke `mcp.ts` fns that *can* drift from REST/UI. | `files-table.tsx:233–249`; `convex/mcp.ts` vs `apiV1.ts`. | Cosmetic label; soften the matrix's drift claim. |
| F21 | P3 | Dev hygiene | The **dev** deployment DB is littered with test-artifact events (`Alpha Summit`, `Beta Kickoff`, `Scoped Event`, `ne`, duplicate `Devcon Berlin`) and throwaway API keys (`scope-test`, `probe-mcp-*`, `mcp-e2e-proof`). Dev-only, no prod impact, but noisy; `seed:setup` purges artifact events but was not recently run. | `runOneoffQuery` over dev `events`/`apiKeys`. | Reseed dev; revoke leftover dev keys. |

**Not a finding (self-inflicted, cleaned):** during this review a sub-probe wrote `scripts/_adv_*.mjs` containing a plaintext dev key and I wrote transient `*.mjs` audit scripts; all were removed and the dev key revoked. Working tree verified clean (`git status` empty). Flagged only so it is not mistaken for a pre-existing repo-hygiene defect.

---

## Per-attack-surface verdicts

1. **Prompt-by-prompt compliance** — **partial** (see the not-fully-honored list below): F5, F14, F16 + several partials; the big-ticket directives (identity model, per-member event scoping, single-source speaker system, unique-link architecture, workspace switcher, MCP gating, copilot panel width, Connect-MCP 2-icons, right-click-logo, CI/CD + release-to-prod, no-Claude-co-author) are all **honored** and code-verified.
2. **The judge's path** — **CLEAN with 1 P2** (F13). Public home/event/CFP render clean; login → `/app/:ws/:event` works; the 5-step CFP wizard advances a **fresh email straight through** to step 3 with zero console errors; CFP-16 edit-lock is correctly enforced for accepted talks after close (`convex/portal.ts::editLockFor`, enforced at the portal mutation). Full submit→triage→commit→publish→.ics chain was **not** driven end-to-end in one browser session (components verified individually).
3. **The identity model** — **CLEAN.** `submit.identify` returns a token only for a brand-new address or a token-holder; every address with history (submission, co-speaker credit, task, upload, or any filled profile field) gets a rate-limited (≤3/hr) emailed sign-in link and a response that leaks no token/name/drafts/counts. Verified live: fresh email → straight through. Minor by-design note: the `ready` vs `link_sent` response distinguishes "new" from "known" address (unavoidable enumeration oracle; no credential leak).
4. **Multi-tenancy + scoping** — **CLEAN.** `eventAccessFor` returns `"Event not found."` (404, no existence hint) to a scoped-out member across browser, REST, and MCP; unknown slug → 404; cross-workspace non-member → `403 "You don't have access to this workspace."` (standard 403-vs-404 existence tradeoff — minor). Three settings levels are separate pages with correctly-scoped headers.
5. **MCP full proxy + universal gating** — **gating CLEAN; full-proxy CLEAN-with-caveats (F12, F20b).** Live: exactly **81** tools (27 read / 54 write); every write refuses without `confirm:true` (checked before validation, side-effect-free) and executes with it; reads ungated; `delete_event` needs `confirm`+exact `confirmName` (verified created/deleted/refused on dev). Copilot approval chain (`chat.ts` `toolApproval` off the server's `readOnlyHint` → `copilot-mcp.ts` injects `confirm:true` post-approval) verified in code.
6. **REST + OpenAPI truthfulness** — **NOT CLEAN — 5 findings (F1 P0, F4 P1, F6/F7/F17 P2).** Core contract (dual auth forms, `{error,code,message,status}` envelope with **no** prod `[Request ID] Server Error` leak, all 109 routes live, response shapes, capped pagination, webhook HMAC verified end-to-end incl. tamper-reject, `.ics` valid) is honest and well-engineered; the server-URL P0 is the headline.
7. **Eval-kit rubric cross-read** — **parity gaps remain (F9, F10, F11).** Ledger scores confirmed verbatim; reviewer-name-as-raw-email is FIXED; EMB-15, X/Twitter autosave, and profile-task auto-tick are **still real** product gaps; AI-review and co-author role are deliberately out of scope.
8. **Design-system coherence** — **CLEAN** on sampled surfaces: no raw Convex ids or token-like strings rendered, no `role="button"` on links, no horizontal overflow at **1440px or 390px** across public + authed app pages.
9. **Instant-everything (rule 26)** — **CLEAN.** Warm client-side route switches on **prod** measured **29–46ms** (Submissions/Forms/Evaluation/Agenda/Speakers/Communications/Dashboard), **zero** skeletons during transitions, zero console errors.
10. **Copilot** — **CLEAN-with-caveat (F5).** Panel max width `min(900px,55vw)` (exceeds the requested ~40%), Connect-MCP shows 2 icons + "Connect MCP" label, key masked in UI / real on copy, write-approval gating wired. Generative UI is not present for all 81 tools (F5). A live end-to-end copilot chat run (model round-trip) was not executed; the above is code + static verification.
11. **Email + Airtable egress** — **Email NOT CLEAN (F2 P1, F3 P1, F8/F18); Airtable CLEAN (P3 nits).** Resend from `hello@trackstage.app` real on dev+prod; `@example.com`→preview enforced in both send doors; "Hi there" fallback present; deadline + task crons registered and real; all email surfaces exist. Airtable: messy base-id paste parses (30/30 unit tests), errors are `ConvexError` sentences (survive prod redaction), pull-before-push with loop guard, and **prod is NOT in demo mode** (`AIRTABLE_DEMO_MODE` unset on prod; set on dev).
12. **Repo hygiene** — **CLEAN on the things that matter (F15/F16/F21 are P2/P3).** No live secrets in the tree or `PROMPTS.md` (all pasted keys `[REDACTED-SECRET]`), only intended `.env.example`/`.env.production` committed, **no Claude co-author trailer** in 118 commits, **CI green on HEAD**. Stale README facts (F15) and the unpublished video (F16) are the open items.

---

## PROMPTS.md instructions judged NOT fully honored (with evidence)

Verified against code, not docs. The unique instruction set is founding-session #1–#109 + side sessions (the "continuation session" 51 prompts are verbatim duplicates).

1. **#30 — "every individual MCP tool call needs a nice in-app generative UI."** ~50 of 81 tools have bespoke views; the rest fall back to raw JSON. `src/components/copilot/tool-views/registry.tsx:116,242–262`. → **F5.**
2. **#66 — "button looks a bit odd perhaps we should remove that yk yk."** Declare-the-winner button still ships; only its 3D style was retired. `src/routes/index.tsx:17`, `declare-winner-button.tsx:26`. → **F14** (needs Marko confirmation).
3. **#53/#83 — "openapi.json 100% accurate to the actual server."** Prod spec's `servers[0].url` is the **dev** deployment. → **F1 (P0).**
4. **#97 — "MCP can do all the user can do (full proxy)."** `workspaces.update`, trash-listing behind `restore_submission`, and evaluator-token rotation have no MCP tool and aren't in the excluded list. → **F12.**
5. **#73 — "add some yopmail users so i can see the emails arrive."** No code artifact — every seeded recipient is `@example.com` (renders as a preview, never sent). `grep yopmail` → 0 hits. (The dangling-tool-call half of #73 *is* fixed at `src/routes/api/chat.ts:153–155`.)
6. **#77 — task kinds custom vs predefined.** The free-form **`answer`** kind exists end-to-end (honors "custom question"), but the kind set itself is a fixed enum (`profile|headshot|upload|answer|confirm`, `convex/schema.ts:502`) — no organizer-defined kinds. Honored-in-spirit; the literal reading isn't.
7. **#71/#89 — "100% unique link structures, no conflicts."** Architecture is workspace+event scoped and solid, but legacy `/submit/:eventSlug` dead-ends at "couldn't find that call for speakers" rather than resolving to the event CFP. → **F13.**
8. **#27/#52/#90 loop — "100% parity, miss nothing."** Ledger open defects remain: EMB-15 (F11), X/Twitter autosave (F9), profile-task auto-tick (F10); co-author role deliberately deprioritized (swyx struck it to nice-to-have).
9. **#80/#94 — launch video deliverable.** Renders exist but are gitignored and referenced nowhere. → **F16.**
10. **#96 — Airtable "demo mode."** Answered/scoped (per-connection env flag, not "a whole demo-mode project") and prod verified **not** in demo mode — honored, but only confirmable out-of-band (`convex env list --prod`); nothing in the repo pins prod's state.
11. **Rule 18e — "nothing may silently not-send."** Platform-email failures are silent (no durable record/retry). → **F8.**
12. **README accuracy (implied by "super readable / all claims true").** "31 tools" / "129 checks" / wrong `cd` dir. → **F15.**

**Verified honored (spot-checked in code):** per-member event scoping w/ admin picker (#67/#81), MCP key masked-but-copies (#68/#10), single-source `people` speaker system + select-existing PersonPicker (#70/#85/#87), workspace switcher (#79/#21), save-task-to-library via MCP + fixed raw-id label (#78), copilot panel `min(900,55vw)` (#107), Connect-MCP 2 icons + label + one reused dialog (#108/#101), sticky public header + branded portal (#82), right-click-logo→design-system (#6/#22), account-settings-inline page (#69), portal-token identity model (#74), two-way Airtable + agent audit lens (#54/#55), global search wired (#56), OpenAPI auth schemes (#83), refreshed screenshots/docs (#84), CI/CD + release-to-prod (#109), agenda 15-min snap (#35), and **no Claude co-author trailer** anywhere.

---

## Top actions before ship (reviewer's ranking — do NOT auto-apply)

1. **F1 (P0)** — point the published `openapi.json` at the prod site and make `--check --live` assert it, before the API docs mislead a judge or wipe dev data via "Try it."
2. **F2/F3 (P1)** — escape interpolated values in `platformEmails.ts`; decide email HTML-ness from the template, not the merged body. Real content and a real attacker both trigger these on the verified sending domain.
3. **F15/F21 + review artifacts** — correct README's stale facts and reseed/clean dev before the repo flips public.
4. **F9/F10/F11** — the three still-open scored ledger items (X/Twitter clobber, profile-task auto-tick, EMB-15).
5. **F14** — one yes/no from Marko: keep or kill the Declare-the-winner button.
