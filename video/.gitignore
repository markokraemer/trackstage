node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Raw ElevenLabs TTS output (the trimmed/normalized takes live in public/audio/vo)
vo/raw/

# Capture workspace artifacts (recordings are reproducible via capture/*.mjs)
capture/raw/
capture/.auth.json
capture/state.json
capture/marks.json
capture/fr_*.png
capture/probe.mjs
capture/chk*.mjs
capture/recon*.mjs
capture/qa/
capture/qa2/
capture/sheets/
